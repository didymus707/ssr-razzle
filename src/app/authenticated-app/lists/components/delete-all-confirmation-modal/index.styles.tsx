import styled from '@emotion/styled';
import { ModalContent } from '@chakra-ui/core';

export const DeleteAllModalWrapper = styled(ModalContent)`
  border-radius: 10px;
  padding: 30px 20px;
  min-width: 450px;
`;
