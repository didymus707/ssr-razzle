import styled from '@emotion/styled';
import { ModalContent } from '@chakra-ui/core';

export const EnrichConfirmationModalWrapper = styled(ModalContent)`
  border-radius: 10px;
  padding: 40px 20px;
  min-width: 500px;
`;
